apiVersion: v1
kind: Service
metadata:
  name: green-trade-frontend-svc
  namespace: green-trade
  labels:
    app.kubernetes.io/name: green-trade
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/name: green-trade
    app.kubernetes.io/component: frontend
